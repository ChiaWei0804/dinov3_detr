# DINOv3 上游原始碼（Meta 版權，受 DINOv3 License Agreement 約束）
# 這個專案只放自己寫的偵測器，不轉散布 Meta 的程式碼。
# 兩者都是執行時必需，請依 README 的 Setup 從上游取得後放到專案根目錄。
dinov3/
hubconf.py

# 模型權重與 checkpoint（weights/ 有 327MB，不進版控）
weights/
*.pth
*.pt
*.ckpt

# 訓練輸出
runs/

# 簡報素材
ppt/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Claude Code 個人本機覆寫（含本機專屬路徑，不該跨機器共用）
.claude/settings.local.json

# 內部交接文件，不上傳
TRAINING_HANDOFF.md

# OS / 編輯器
Thumbs.db
.DS_Store
.vscode/
.idea/
